// VehicleDetected.tsx
import React, { FC } from 'react';

const VehicleDetected: FC = () => {
  
  const vehicleCount = 0;

  return (
    <div>
      <h2>Vehicle Detected</h2>
      <p>{vehicleCount}</p>
    </div>
  );
}

export default VehicleDetected;
